import 'package:flutter/material.dart';

class AppRadius {
  AppRadius._();
  static final BorderRadius input = BorderRadius.circular(16.0);
  static final BorderRadius button = BorderRadius.circular(16.0);
}
