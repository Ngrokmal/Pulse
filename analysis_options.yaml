include: package:flutter_lints/flutter.yaml

linter:
  rules:
    # Project-specific overrides can go here. Left at flutter_lints defaults
    # for now — tighten or relax per team convention as needed.

analyzer:
  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"
    - "build/**"
