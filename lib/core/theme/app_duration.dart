class AppDuration {
  AppDuration._();
  static const Duration fast = Duration(milliseconds: 120);
  static const Duration medium = Duration(milliseconds: 200);
  static const Duration bearCover = Duration(milliseconds: 350);
  static const Duration blinkInterval = Duration(milliseconds: 3200);
  static const Duration blinkLook = Duration(milliseconds: 150);
}
